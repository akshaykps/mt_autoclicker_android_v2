<?xml version="1.0" encoding="utf-8"?>
<!-- Live capturing state -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="48dp"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="2dp"
    android:paddingEnd="2dp">

    <ImageView
        android:id="@+id/notif_ss_cap_dot"
        android:layout_width="10dp"
        android:layout_height="10dp"
        android:layout_marginEnd="8dp"
        android:contentDescription="@null"
        android:src="@drawable/ic_capture_glow" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/notif_ss_cap_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:maxLines="1"
            android:text="Capturing full page…"
            android:textColor="#FF1C1C1E"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/notif_ss_cap_body"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="Scrolling &amp; stitching — don’t touch the screen"
            android:textColor="#FF6B6B70"
            android:textSize="12sp" />
    </LinearLayout>

    <TextView
        android:id="@+id/notif_ss_cap_frames"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:fontFamily="sans-serif-medium"
        android:maxLines="1"
        android:text="0"
        android:textColor="#FF0891B2"
        android:textSize="13sp" />
</LinearLayout>
